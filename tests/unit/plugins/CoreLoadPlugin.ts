import { describe, it } from 'intern!bdd';
import  '../../../src/plugins/CoreLoadPlugin';

const { basePath: p = '' } = {};

console.log(`test basePath is ${p}`);

describe('core-load', () => {
	it('should add event listeners', () => {
	});
});
